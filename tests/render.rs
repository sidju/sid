use sid::*;

use std::collections::HashMap;

pub fn render_test_fixture(
  template: Template,
  parent_stack: Vec<DataValue>,
  parent_scope: &HashMap<String, RealValue>,
  global_scope: &HashMap<String, RealValue>,
  expected_rendered_stack: Vec<DataValue>,
) {
  todo!()
}
